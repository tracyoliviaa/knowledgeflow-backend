nelmio_cors:
    defaults:
        origin_regex: true
        allow_origin: ['*']  # Temporarily allow all for testing
        allow_methods: ['GET', 'POST', 'PUT', 'PATCH', 'DELETE', 'OPTIONS']
        allow_headers: ['Content-Type', 'Authorization']
        expose_headers: ['Link']
        max_age: 3600